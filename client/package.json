{
  "name": "rpg-magic-client",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "No-build client: prebuilt ES modules served statically. See README for why.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "python3 -m http.server 5173 --directory ..",
    "build:validator": "node tools/build-validator.js"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  }
}
